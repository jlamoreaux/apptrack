# scripts/*.sql — frozen

> **FROZEN — HISTORICAL ONLY. DO NOT RUN, DO NOT EDIT.**
>
> These files are no longer authoritative. They are three uncoordinated directories with no migration
> ledger, and they have provably diverged from production. Verified examples:
>
> - `handle_new_user` has six conflicting definitions here; production runs a seventh, simpler one that
>   inserts only the `profiles` row.
> - `handle_new_user_subscription()` exists in production but is wired to **no trigger** and has never fired.
> - `schemas/migrations/014` defines a materialized view later corrected by `017`; production runs `017`.
> - `schemas/migrations/018` defines `get_next_display_order` with `FOR UPDATE` on an aggregate, which
>   Postgres rejects at runtime. Every call has always failed.
> - Tables `subscriptions` and `career_advice_sessions` are referenced by code but do not exist.
>
> **The authoritative schema is `db/prod-truth/`**, regenerated by `scripts/migration/dump-prod-truth.sh`.
> From Stage 1 onward, schema changes go through `drizzle-kit generate` / `drizzle-kit migrate` only.

